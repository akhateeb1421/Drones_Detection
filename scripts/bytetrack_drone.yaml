# ByteTrack config for the drone-defense pipeline.
# Same numbers as the Colab notebook so we keep behavior consistent.
tracker_type: bytetrack
track_high_thresh: 0.35
track_low_thresh: 0.05
new_track_thresh: 0.35
track_buffer: 300
match_thresh: 0.8
fuse_score: True
